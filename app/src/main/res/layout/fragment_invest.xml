<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black">

    <TextView
        android:layout_width="0dp"
        android:layout_height="56dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:text="@string/invest"
        android:textColor="@color/white"
        android:textSize="25dp"
        android:fontFamily="@font/sf_bold"
        android:gravity="center"
        android:background="@color/gray"
        android:id="@+id/study_title_tv"/>

    <ImageView
        android:id="@+id/invest_title_iv"
        android:layout_width="212dp"
        android:layout_height="113dp"
        android:src="@drawable/daysinvest"
        app:layout_constraintTop_toBottomOf="@id/study_title_tv"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="120dp"/>

    <Button
        android:id="@+id/invest_start_quiz_btn"
        android:text="시작하기"
        android:fontFamily="@font/sf_bold"
        android:textSize="30sp"
        android:textColor="@color/white"
        android:background="@drawable/buttom_round"
        android:layout_width="150dp"
        android:layout_height="150dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/invest_title_iv"
        android:layout_marginTop="30dp">
    </Button>

</androidx.constraintlayout.widget.ConstraintLayout>